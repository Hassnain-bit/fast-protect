{
  "scripts": {
    "build-serve": "cd ../frontend && npm i && npm run build && cd ../backend && npm i && node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
